---
apiVersion: troubleshoot.sh/v1beta2
kind: SupportBundle
metadata:
  name: supportbundle
spec:
  collectors:
    - clusterResources:
        exclude: true
    - helm:
        releaseName: YOUR-RELEASE-NAME-HERE
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        collectValues: true


    - exec:
        name: logs/access-control
        selector:
          - app.kubernetes.io/name=access-control
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: access-control
        includeAllData: true


    - exec:
        name: logs/change-data-capture
        selector:
          - app.kubernetes.io/name=change-data-capture
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: change-data-capture
        includeAllData: true


    - exec:
        name: logs/debezium-service
        selector:
          - app.kubernetes.io/name=debezium-service
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: debezium-service
        includeAllData: true


    - exec:
        name: logs/gateway
        selector:
          - app.kubernetes.io/name=gateway
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: gateway
        includeAllData: true


    - exec:
        name: logs/manager
        selector:
          - app.kubernetes.io/name=manager
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: harness-manager-config
        includeAllData: true


    - exec:
        name: logs/log-service
        selector:
          - app.kubernetes.io/name=log-service
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: log-service
        includeAllData: true


    - exec:
        name: logs/migrator
        selector:
          - app.kubernetes.io/name=migrator
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: migrator
        includeAllData: true


    - exec:
        name: logs/ng-custom-dashboards
        selector:
          - app.kubernetes.io/name=ng-custom-dashboards
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ng-custom-dashboards
        includeAllData: true


    - exec:
        name: logs/ng-dashboard-aggregator
        selector:
          - app.kubernetes.io/name=ng-dashboard-aggregator
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ng-dashboard-aggregator
        includeAllData: true


    - exec:
        name: logs/ng-manager
        selector:
          - app.kubernetes.io/name=ng-manager
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ng-manager
        includeAllData: true


    - exec:
        name: logs/pipeline-service
        selector:
          - app.kubernetes.io/name=pipeline-service
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: pipeline-service
        includeAllData: true


    - exec:
        name: logs/platform-service
        selector:
          - app.kubernetes.io/name=platform-service
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: platform-service
        includeAllData: true


    - exec:
        name: logs/policy-mgmt
        selector:
          - app.kubernetes.io/name=policy-mgmt
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: policy-mgmt
        includeAllData: true


    - exec:
        name: logs/scm
        selector:
          - app.kubernetes.io/name=scm
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: scm
        includeAllData: true


    - exec:
        name: logs/service-discovery-manager
        selector:
          - app.kubernetes.io/name=service-discovery-manager
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: service-discovery-manager
        includeAllData: true


    - exec:
        name: logs/template-service
        selector:
          - app.kubernetes.io/name=template-service
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: template-service
        includeAllData: true


    - exec:
        name: logs/gitops
        selector:
          - app.kubernetes.io/name=gitops
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: gitops
        includeAllData: true


    - exec:
        name: logs/et-service
        selector:
          - app.kubernetes.io/name=et-service
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: et-service
        includeAllData: true


    - exec:
        name: logs/et-collector
        selector:
          - app.kubernetes.io/name=et-collector
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: et-collector
        includeAllData: true


    - exec:
        name: logs/ff-service
        selector:
          - app.kubernetes.io/name=ff-service
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ff-service
        includeAllData: true


    - exec:
        name: logs/ff-pushpin-service
        selector:
          - app.kubernetes.io/name=ff-pushpin-service
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ff-pushpin-service
        includeAllData: true


    - exec:
        name: logs/cv-nextgen
        selector:
          - app.kubernetes.io/name=cv-nextgen
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: cv-nextgen
        includeAllData: true


    - exec:
        name: logs/le-nextgen
        selector:
          - app.kubernetes.io/name=le-nextgen
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: le-nextgen
        includeAllData: true


    - exec:
        name: logs/learning-engine
        selector:
          - app.kubernetes.io/name=learning-engine
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: learning-engine
        includeAllData: true


    - exec:
        name: logs/srm-ui
        selector:
          - app.kubernetes.io/name=srm-ui
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: srm-ui
        includeAllData: true


    - exec:
        name: logs/verification-svc
        selector:
          - app.kubernetes.io/name=verification-svc
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: verification-svc
        includeAllData: true


    - exec:
        name: logs/sto-manager
        selector:
          - app.kubernetes.io/name=sto-manager
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: sto-manager
        includeAllData: true


    - exec:
        name: logs/sto-core
        selector:
          - app.kubernetes.io/name=sto-core
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: sto-core
        includeAllData: true


    - exec:
        name: logs/nextgen-ce
        selector:
          - app.kubernetes.io/name=nextgen-ce
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: nextgen-ce
        includeAllData: true


    - exec:
        name: logs/anomaly-detection
        selector:
          - app.kubernetes.io/name=anomaly-detection
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: anomaly-detection
        includeAllData: true


    - exec:
        name: logs/batch-processing
        selector:
          - app.kubernetes.io/name=batch-processing
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: batch-processing
        includeAllData: true


    - exec:
        name: logs/cloud-info
        selector:
          - app.kubernetes.io/name=cloud-info
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: cloud-info
        includeAllData: true


    - exec:
        name: logs/event-service
        selector:
          - app.kubernetes.io/name=event-service
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: event-service
        includeAllData: true


    - exec:
        name: logs/ng-ce-ui
        selector:
          - app.kubernetes.io/name=ng-ce-ui
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ng-ce-ui
        includeAllData: true


    - exec:
        name: logs/telescopes
        selector:
          - app.kubernetes.io/name=telescopes
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: telescopes
        includeAllData: true


    - exec:
        name: logs/ng-ce-ui
        selector:
          - app.kubernetes.io/name=ng-ce-ui
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ng-ce-ui
        includeAllData: true


    - exec:
        name: logs/looker
        selector:
          - app.kubernetes.io/name=looker
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: looker
        includeAllData: true


    - exec:
        name: logs/chaos-web
        selector:
          - app.kubernetes.io/name=chaos-web
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: chaos-web
        includeAllData: true


    - exec:
        name: logs/chaos-manager
        selector:
          - app.kubernetes.io/name=chaos-manager
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: chaos-manager
        includeAllData: true


    - exec:
        name: logs/chaos-linux-ifs
        selector:
          - app.kubernetes.io/name=chaos-linux-ifs
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: chaos-linux-ifs
        includeAllData: true


    - exec:
        name: logs/chaos-linux-ifc
        selector:
          - app.kubernetes.io/name=chaos-linux-ifc
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: chaos-linux-ifc
        includeAllData: true


    - exec:
        name: logs/chaos-k8s-ifs
        selector:
          - app.kubernetes.io/name=chaos-k8s-ifs
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: chaos-k8s-ifs
        includeAllData: true


    - exec:
        name: logs/ci-manager
        selector:
          - app.kubernetes.io/name=ci-manager
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ci-manager
        includeAllData: true


    - exec:
        name: logs/ti-service
        selector:
          - app.kubernetes.io/name=ti-service
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ti-service
        includeAllData: true


    - exec:
        name: logs/ssca-manager
        selector:
          - app.kubernetes.io/name=ssca-manager
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ssca-manager
        includeAllData: true


    - exec:
        name: logs/ssca-ui
        selector:
          - app.kubernetes.io/name=ssca-ui
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        command:
          - "/bin/bash"
          - "-c"
          - |
            logs_path="/opt/harness/logs"
            num_files=3

            for file in $(ls $logs_path/pod*.log | sort -n -k1,1 | tail -n $num_files); do
                echo "==> $file &lt;=="
                cat "$file"
            done
        timeout: 30s

    - configMap:
        namespace: YOUR-RELEASE-NAMESPACE-HERE
        name: ssca-ui
        includeAllData: true

